* install wxPython
apt-get install python-wxgtk2.8 python-wxtools wx2.8-doc wx2.8-examples wx2.8-headers wx2.8-i18n
* install gnome
apt-get install gnome-panel
* install python pip and requests
apt-get install python-pip
pip install requests
* install memcached
apt-get install memcached python-memcache

* webkit
apt-get install python-webkit

* mysql client
apt-get install mysql-client libmysqlclient-dev
apt-get install python-dev python-setuptools
easy_install mysql-python

* auto login
system tools -> system settings -> user accounts

* startup app
system tools -> preferences -> startup applications

* booting text
/lib/plymouth/themes/ubuntu-text/ubuntu-text.plymouth

* sudo without password
lomotime ALL=(ALL) NOPASSWD: ALL

* no auto upgrade

* wlan0 config
vi /etc/modprobe.d/iwlwifi.conf
	options iwlwifi 11n_disable=1

* wifi manager
apt get install wicd-gtk
# need to turn off the built-in network manager

* brightness and lock
no lock

* soft keyboard
always on top

* hide taskbar
left-alt + win + right-click

* jdk
apt-get install openjdk-6-jdk
